# inbox 2026-06

## 2026-06-01 14:30

来源：GitHub 周榜文章分析 + 5 个项目实测

分析了 GitHub 周榜 10 个项目中值得关注的 5 个，全部完成安装 + 真实验证 + 共享。

**实测结果速览：**

| 项目 | 星数 | 安装方式 | 关键发现 |
|------|------|---------|---------|
| Understand-Anything | 45.9k | `curl|bash -s hermes` | 111 文件 JS 项目 8 分钟出图谱，115 节点 135 边 7 层。Dashboard 交互式浏览 |
| markitdown | 132.4k | `pip install` | 微软官方，HTML/JSON/TXT 转纯净 Markdown 一次过 |
| ECC | 199.3k | git clone → symlink | 249 skills, 63 agents。brand-voice 和 cost-aware-llm-pipeline 最值看 |
| knowledge-work-plugins | 18.2k | git clone → symlink | Anthropic 20 插件，draft-content 实测写了博客，stop-slop 自检 42/50 |
| stop-slop | 7.4k | 下载 SKILL.md | 去 AI 写作痕，10 维度评分体系。和 ECC brand-voice 互补 |

**三件套管线：** Understand-Anything(理解) → ECC(执行) → markitdown(数据)。三个能力层刚好拼成 Agent 基础设施。

**ECC 技能库图谱化：** 用 Understand-Anything 分析了 ECC 全部 249 个 skill，建成 262 节点知识图谱，13 分类层，Dashboard 可交互浏览（http://localhost:5175/?token=52386db23ab75839dea6900606dd18dd）。

**learnings：**
- taste-skill 实际是前端 UI 设计框架，非内容质量工具——skip
- ECC V2.0.0-rc.1 有原生 Hermes 接入指南
- WSL npm 安装超时是代理 IP 漂移问题，调试后恢复
- 共享仓库（~/.shared-skills/devops/）已更新 5 个新 skill

eval: done | 已归档

## 2026-06-01 15:00

来源：ECC continuous-learning-v2 在 Hermes 上的场景映射

ECC 的 instinct 学习模型（hook 自动观察 → 原子行为 → 置信度打分 → 进化成 skill），在 Hermes 上可映射为：

**已自动工作的场景：**
- 用户纠正操作方式（"不要用 cat，用 read_file"）→ 自动存 memory ✅
- 重复流程固化 → 手动 skill_manage(action='create') 存为 skill

**可手动做的事情：**
- 周末 session_search(query="completed OR corrected") → 发现重复模式 → 存 skill
- 跨项目模式晋升（2+ 项目都出现同一偏好 → 晋升到 shared-skills）

**差异：**
- ECC：全自动 hook 驱动，但有噪声（249 skill 中不少 low-confidence）
- Hermes：半自动，每次修正都有 real signal。更适合对质量有要求的场景

eval: done | 已追加

## 2026-06-02 14:30

来源：X 转推 @GoSailGlobal（Jason Zhu）2026-05-31，X 数据 18h 前 / 19 评 / 75 转发 / 225 赞 / 14K 浏览

Jason Zhu 转推了一条关于「中医 Agent Skill」的项目：有人把倪海厦 12 门课全部灌进 Claude Code，做成 domain-specific Agent Skill。

这个 pattern 跟我今天用的「Understand-Anything 把 ECC 249 个 skill 建成知识库」是同一类：**专家知识库 + Claude Code = 领域垂直 agent**。但更狠——它把整个学派（倪海厦中医经方派）压缩成一个可调用的 skill。

如果把这种"垂直知识 agent"的范式拆开看：
1. 知识源：单一权威（倪海厦 12 课，非大模型数据全集）
2. 表达层：把文本切成可检索的子知识
3. 推理层：让 Claude Code 知道什么时候调用
4. 验证层：人/医生 review 答案

这套范式可迁移到：
- 法学（一个学派、一位法官的判决风格）
- 投资（一个策略师的分析框架）
- 设计（一种设计哲学的语料）
- 内容创作（一个细分领域的全部代表作）

**核心问题**：这种"垂直知识 agent"和"通用 agent + RAG 检索"本质区别是什么？是不是把"什么时候用什么知识"也编码进了 agent 的 system prompt，而不只是检索到文档？

我想让 AI 帮我做：
- 找这个中医 Agent Skill 的原仓库（GitHub 链接），看 prompt 结构和切分策略
- 评估能否用 china-ai-frontline 的「Knowledge-Work-Plugins 18.2k」做类似 demo
- 写一篇「领域垂直 agent 的最小可行解」X thread（如果素材够）

eval: done | 仓库已找到 (JuneYaooo/nihaisha-tcm ⭐295)，已在 PR #41/#42 沉淀

---

## 2026-06-02 Coze 3.0 直播观感

来源：Coze 3.0 直播（B 站 / 抖音同步），2026-06-02
首发平台：待定
audience：AI builder + 关注国内 AI 产品 PMF 的从业者

看了 Coze 3.0 直播。最打动我的不是技术参数，而是产品定位的直觉对了。

真正面对大众市场的 AI 产品，特征不是参数最大、模型最新，而是三个词：**低技术门槛、好泛化、真实工作场景**。

Coze 3.0 的 skill store 里充斥着这样的技能包：

- **法务类案生成**：输入案情摘要，输出类案检索报告
- **公众号爆款写作**：给素材，按爆款结构出排版好的文章
- **金融市场分析**：自然语言描述标的需求，输出研报摘要 + 数据可视化

这些 skill 的共同点：
1. 用户**不用写代码**——没有 prompt engineering，没有 API 调优
2. **不用从零找脚手架**——技能包预装，即开即用
3. **自然语言就够了**——用"帮我写一个……"替代写 prompt 模板
4. 每个 skill 锚定一个**真实岗位的真实痛点**——不是"AI 能做什么"，而是"我的工作卡在哪"

这不只是"AI 降低门槛"，这是 **PMF 的落地路径变了**：

- 第一波（2023-2024）：模型能力展示 → "看，它能写诗！" → 没有工作流嵌入
- 第二波（2024-2025）：Agent 框架 → "你可以搭一个 workflow" → 需要技术理解
- 第三波（2026-）：Skill 商店 → "你的工作，帮你做完" → 零技术假设

Coze 3.0 让我重新想一个问题：**在一个产品里，模型能力是基础设施，不是卖点。卖点是"打开就能替我做一部分工作"。**

这和 china-ai-frontline 做 domain-specific skill 的直觉一致——只不过 Coze 的平台化路径是让用户发现/安装 skill，我们做的是为特定内容工作者定制化。殊途同归。

另外注意到一个细节：Coze 3.0 的 skill 大多不是"通用 AI 能力"，而是"行业 knowledge + AI 执行"的封装。法务 skill 不只是调用 LLM，而是内置了类案检索的检索策略、裁判要旨的提取模板、判决书结构的知识。**Know-how 在 skill 里，不在用户的 prompt 里。**

这可能是国内 AI 产品走向 PMF 的关键转折。

---

**想让 AI 帮我想清楚的事：**

1. **Coze skill → 本地 agent 技能库**：Coze 上现成的行业 skill（法务类案、公众号爆款、金融分析）能不能反哺 OpenClaw / Hermes 这些还没有"脚手架"的 agent 框架？让本地 agent 也可以快速装配成某个领域的专家，而不是从零搭 workflow。
2. **Coze agent 与本地终端 agent 的协作模式**：Coze 类的 cloud agent 当"行业专家层"（懂法务、懂金融），执行层调本地 agent（文件读写、浏览器操作、代码执行）——能否通过 API 或 message platform 实现这种快速合作？coze 出 know-how，本地出行动力。
3. **产品观念的映射**：Coze 3.0 反映了什么样的市场需求和产品观念？是"AI 能力过剩，应用层缺位"的必然结果，还是大厂第一次真正理解了 B 端用户不是要 AI 而是要"帮我做完一件事"？这对 openclaw/hermes/reasonix 这层工具型 agent 意味着什么——是互补关系，还是终局会被平台吃掉？

---

## 2026-06-03 Coze skill → 本地 agent 反哺路径调研

来源：inbox/2026-06.md#2026-06-02-14-30（Coze 3.0 直播观感延伸）

**核心问题**：Coze 上的行业 skill（法务类案、公众号爆款、金融研报）能否移植到 OpenClaw / Hermes，让本地 agent 不用从零搭 workflow？

**调研结论**：分四类反哺路径，技术可行性递减。

### 路径 A：直接 fork skill 仓库（可行，难度低）

Coze 3.0 的 skill 大多开放源码或导出 JSON 定义。
- ✅ 法务类案 skill：本质是 prompt 模板 + 检索策略 → 可平移成 references/ 类案检索.md
- ✅ 公众号爆款 skill：标题模板 + 段落结构 + 排版规则 → 可平移成 draft-content skill 的子模块
- ✅ 金融研报 skill：分析师 prompt + 数据源 API → 可平移成 references/ 研报结构.md
- 难点：Coze skill 假设了字节内部工具链（豆包/飞书），需要剥离后才能在本地 agent 跑

### 路径 B：MCP 协议桥接（可行，难度中）

Coze 3.0 已支持 MCP server 暴露。
- 本地 agent（Hermes / OpenClaw）通过 `.mcp.json` 订阅 Coze 暴露的行业 skill
- 调用模式：本地 user query → 本地 agent 决策 → MCP call → Coze skill 回答
- 限制：每次调用消耗 Coze 平台 token / 算力 + 延迟 + 网络
- 适用：偶发性专业咨询（"帮我审一份合同"），不适合日常高频操作

### 路径 C：skill 知识蒸馏后离线部署（可行，难度高，价值最大）

把 Coze skill 的 prompt + 检索策略 + 知识库 → 蒸馏成本地 SKILL.md + references/。
- 这正是昨天调研的"领域垂直 agent"路径，Coze 帮我们做了"知识发现"工作
- 步骤：clone Coze 公开 skill → 拆解 prompt 结构 → 改写为 SKILL.md 路由表 → 喂入 references/
- 价值：本地 agent 永久拥有该能力，无外部依赖，无 token 成本
- 案例：Coze 上的"法务类案生成" skill 可蒸馏成本地 hermes-lawyer skill

### 路径 D：行业 know-how 反向沉淀（不可行但值得借鉴）

Coze 上沉淀的行业 know-how（裁判要旨提取模板、研报结构、爆款节奏），
无法直接复制到本地 agent，但**写作框架**可借鉴。
- 启示：Coze skill 的"行业 knowledge + AI 执行"封装思路，与本地 agent 的 SKILL 模式高度同构
- 未来：行业 know-how 本身可能成为稀缺商品，agent 平台之间的差异在"行业 know-how 库"

### 三个判断

1. **路径 C 是最大价值**——把 Coze 当"行业 skill 发现引擎"，蒸馏成本地资产
2. **路径 B 适合早期探索**——MCP 桥接快速验证 skill 价值，再决定是否蒸馏
3. **路径 A 是快速 win**——开源 skill 直接 fork，零成本补齐本地能力

**对 OpenClaw / Hermes 的具体动作建议**：
- 选 1 个 Coze 高 star skill（如金融研报）跑路径 A demo
- 把 demo 流程（clone → 拆解 → 改写 → 装入）写进 `china-ai-frontline/posts/` 作技术内容
- 跑通后把"Coze 行业 skill 蒸馏模板"存为 shared skill（~/.shared-skills/）

eval: done | 路径 A 真实跑通 (wechat-viral-title, huajianjiu000/coze-skills, 0 token 装入 hermes)

---

## 2026-06-03 Coze × 本地 agent 协作模式探索

来源：inbox/2026-06.md#2026-06-02-14-30（Coze 3.0 直播观感延伸）

**核心问题**：能否让 Coze 类 cloud agent 当"行业专家层"（懂法务、懂金融），执行层调本地 agent（文件读写、浏览器操作、代码执行）——cloud 出 know-how，本地出行动力？

**架构图（脑内）**：

```
[用户 Slack/微信 query]
     ↓
[本地入口 agent (OpenClaw/Hermes)]
     ↓ 判断：需要专业能力
     ↓
     ├── 简单任务 → 本地 SKILL 直接处理（路由式 references/）
     └── 专业任务 → Coze MCP bridge 调用行业 skill
              ↓
              ├── 答案 + 引用 + 证据
              ↓
         [本地 agent 执行后续]
              ↓
              ├── 写到 inbox/drafts/
              ├── 推到 PR
              ├── 发朋友圈
              └── 任何本地能做的动作
```

### 三种协作模式对比

| 模式 | Cloud agent 角色 | Local agent 角色 | 适用场景 |
|---|---|---|---|
| **B1: Cloud 答 + Local 做** | 行业问答（"这份合同有什么风险"） | 接收答案 + 写文件/发推送/做后续动作 | 偶发专业咨询 |
| **B2: Local 问 + Cloud 查** | 知识检索（"近 3 年同类案判决"） | 拆解 query + 拼装 prompt + 跑后续流程 | 高频研究类工作流 |
| **B3: Local 串 + Cloud 评** | 质量审核（"这段话像不像 AI 写的"） | 内容生成主流程 | 内容创作 pipeline |

### 已有技术组件

- **MCP 协议**：Anthropic 2024-11 开源，Coze 3.0 已支持，Claude Code 客户端原生支持
- **Message platform 桥**：Coze bot 可发布到飞书/微信，Hermes Gateway 可桥接 Slack/Discord
- **Skill 描述协议**：Coze skill 的 JSON 定义与 Claude Code SKILL.md frontmatter 高度同构

### 三个关键卡点

1. **延迟**：本地 agent → Coze MCP → 字节算力 → 返回结果，单跳 2-5 秒
   - 解决：本地 agent 异步处理，用户感知不到
2. **成本**：Coze 调用按 token 计费，高频任务成本爆炸
   - 解决：低频走 Coze，高频蒸馏成本地 SKILL（回到路径 C）
3. **数据隐私**：法务/医疗类问题上传到 Coze 平台
   - 解决：本地敏感数据走本地 skill，仅在脱敏 query 时调 Coze

### 与 Reasonix / Hermes 现有架构的关系

- **OpenClaw** (18789): 已经能调外部 API，是天然的 bridge layer
- **Hermes Gateway** (8642): 已支持多 message platform，可作 Coze bot 与本地 agent 间的消息路由
- **Reasonix** (cache-first loop): 适合做"高频 know-how 缓存层"——Coze 答案首次落本地，后续直接读 cache

**不是替代，是分层**：
- Coze 平台 = 行业 know-how 集市（高频更新、用户共创、按调用付费）
- 本地 agent = 个人能力基础设施（隐私、低延迟、永久资产）
- Bridge = 双向 sync 层（按需调用 + 离线蒸馏）

### 一个具体场景

**跨境电商法务咨询**：
1. 用户 Slack 问："这个 Amazon 卖家合同有坑吗？"
2. Hermes Gateway 收到 → 转 OpenClaw
3. OpenClaw 判断：需要法务能力 → Coze MCP bridge
4. Coze "法务类案" skill 返回风险点 + 类似案例 + 建议条款
5. OpenClaw 写 `drafts/contract-review-{date}.md`
6. Hermes Gateway 推 Slack 给用户 + 提交 PR 到 china-ai-frontline
7. 用户在 Slack 回复 "👍" → 走 express lane commit

整个流程对用户是无感的——他只看到"我问了，agent 答了，文件存了"。

### 待验证问题

- ❓ Coze MCP server 是否对外开放注册？（需要查 Coze 文档）
- ❓ Claude Code 是否能直接订阅任意 MCP server？（答案：能，.mcp.json 配置）
- ❓ 本地 agent 调 MCP 的 latency 在 WSL 网络下实测多少？

**下一步**：选 1 个非敏感场景（如"公众号爆款标题生成"）跑通端到端 demo。

eval: done | 端到端跑通 (create_bot→publish_bot→chat_with_bot, bot 7647398669641826350)，下一个 todo 在 268 行

---

## 2026-06-03 Coze MCP 端到端 demo 真实跑通

来源：inbox/2026-06.md#2026-06-03（Coze skill → 本地 agent 反哺路径调研 — 路径 B MCP 端到端验证）

**核心结果**：用 `coze-dev/coze-mcp-server` v1.27.2 + 真实 SAT token，跑通 `create_bot → publish_bot → chat_with_bot` 全链路。

**端到端调用证据**（所有 isError: false）：

1. `create_bot`:
   - 输入: `{workspace_id, name, description, prompt}`
   - 输出: `bot_id = 7647398669641826350`
2. `publish_bot`:
   - 输入: `{bot_id}`
   - 输出: `version = 1780548856169`
3. `chat_with_bot`:
   - 输入: `{bot_id, user_id, content: "用一句话解释中医里的'阴阳'是什么"}`
   - 输出: "中医里的阴阳是概括事物对立统一属性的核心理论，指导辨证施治，统摄寒热、表里等范畴。"

**关键发现**：
- ✅ Coze MCP server 真实可用（uvx 装 44 包 / `~/.hermes/profiles/w-hermes/mcp.json` 配置好）
- ✅ `user_env: ["COZE_API_TOKEN"]` 模式让 token 不进配置文件
- ✅ `create_bot` 只需要 `workspace_id` + `name`，**比 UI 路径简单 100 倍**
- ✅ `chat_with_bot` 返回真实 LLM 推理结果（非模板）
- ❌ 之前用 bb-browser UI 自动化创建 bot 卡 4+ 次（React 表单 / 多 dialog portal / name 字段填不进去）— **MCP 是正确路径**

**和路径 A（直接 fork skill）的对比**：
- 路径 A 装入 0 token / 0 网络的纯本地 skill（已跑通 #7 wechat-viral-title）
- 路径 B 通过 MCP 调云端 bot（真实 LLM + 真实工作流 + 真实 RAG）
- 路径 A 适合"模板填空"类需求，路径 B 适合"需要 LLM 推理"类需求
- 两者**不互斥**：本地 skill 蒸馏后可用 MCP 暴露给 Claude Code

**产出**：
- 装入位置: `~/AppData/Local/hermes/profiles/w-hermes/mcp.json`
- 凭据: `~/AppData/Local/hermes/profiles/w-hermes/.env` (`COZE_API_BASE`, `COZE_API_TOKEN`)
- Bot 真实存在: `https://www.coze.cn/space/7647333402391379983/develop` (workspace `7647333402391379983`)
- Bot ID: `7647398669641826350` (simple-bot-2026-06-03)

eval: done | 端到端 MCP 真实跑通

## 2026-06-03 Coze 3.0 产品观念映射分析

来源：inbox/2026-06.md#2026-06-02-14-30（Coze 3.0 直播观感）

**核心问题**：Coze 3.0 反映的市场需求和产品观念是什么？对一个工具型 agent（openclaw/hermes/reasonix）来说，平台终局是吃掉我们，还是互补共存？

### Coze 3.0 在卖什么（不是技术参数）

三个关键词：**低技术门槛、好泛化、真实工作场景**。

- 不卖"我们的模型多大"
- 卖"打开就能替我做一部分工作"
- 模型能力是基础设施，不是卖点

这是 B 端用户要的东西——"帮我做完一件事"，不是"给我 AI"。

### 平台经济学的两阶段判断

**短期（1-2 年）：平台 + 工具型 agent 互补**

平台做：行业 skill 集市 + 用户共创 + 按调用付费
工具型 agent 做：个人工作流 + 隐私数据 + 长期资产沉淀

理由：
- 平台擅长"广度"（千万用户、千万场景），不擅长"个人化深度"
- 工具型 agent 擅长"个人流"，不擅长"行业 know-how 库"
- 用户既需要 100 个行业 skill 偶尔调用，也需要 1 个深度适配自己工作流的本地 agent

**长期（3-5 年）：终局被吃掉的风险点**

风险 1：平台下沉到个人 agent 层
- 字节有豆包 + 飞书 + Coze + 抖音 → 全栈打通
- 个人工作流被平台化（"你的工作流在飞书里"）
- 本地 agent 退化为"平台 API 的薄封装"

风险 2：行业 know-how 库成为稀缺资源
- 平台凭用户量沉淀的 know-how 库，个人无法追赶
- Coze skill store 形成"行业最佳实践网络效应"
- 本地 agent 的"个人 know-how"价值相对缩小

风险 3：本地算力成本无法与云端竞争
- 1M context 长文本推理在云端 GPU 上效率 5-10 倍于本地
- 本地 agent 只能依赖本地小模型或购买云端算力 → 又被平台化

### 为什么工具型 agent 还有机会

**个人 agent 的不可替代性**：

1. **隐私与合规**：法务、医疗、财务数据不能上云
2. **延迟与离线**：本地 agent 不依赖网络，断网仍可用
3. **个性化深度**：用户用 6 个月训练出来的 workflow，比平台通用 skill 强 10 倍
4. **跨平台资产**：本地 agent 沉淀的 skill 是用户资产，不被任何平台锁定
5. **创作主权**：内容创作者不愿把"未发布草稿"放在云端

**平台吃掉的是"通用"部分，个人 agent 守住的是"个性"部分**。

### 对 OpenClaw / Hermes / Reasonix 的具体含义

| 项目 | 平台威胁 | 机会 | 战略建议 |
|---|---|---|---|
| **OpenClaw** | 字节全栈下沉 | 个人 API 编排仍稀缺 | 深耕"个人 API 编排"差异化，不做平台化 |
| **Hermes** | 平台自带 message platform | 跨平台 message 桥是稀缺品 | 守住"个人跨平台通信中枢"定位 |
| **Reasonix** | 云端长 context 推理更便宜 | cache + 隐私 + 离线 | 主打"本地 cache 永久资产"，不与云端拼算力 |

**三个战略动作**：

1. **不做通用平台**——平台战争是字节腾讯阿里的事，工具型 agent 做了也输
2. **深耕个人深度**——workflow 个性化是平台不愿做的（千人千面没规模效应）
3. **强化资产沉淀**——用户的 skill / knowledge / context 是个人资产，本地 agent 是唯一可信的"保险柜"

### 一个反直觉的判断

**工具型 agent 的真正对手不是 Coze，是"用户懒得配置"。**

Coze 吃掉的是"懒得配置 + 通用需求"用户。
工具型 agent 服务的是"愿意花 3 小时配置一次 + 个性化需求"用户。

后者是少数派，但 LTV（终身价值）远高于前者。
——这正是 2-8 法则：20% 的深度用户贡献 80% 的价值。

**所以终局判断**：**不是被吃掉，是分层共存**。
- Coze 类平台 = 浅层广度（80% 用户）
- 工具型 agent = 深度个性（20% 用户）
- 两者不竞争，甚至可以合作（蒸馏、桥接）

### 待验证的判断

- ❓ 字节是否会做"个人 agent"产品？（观察豆包 + 飞书 + Coze 整合动向）
- ❓ 20% 深度用户的市场规模是否够支撑工具型 agent 公司？（看 Linear / Notion / Figma 的客户结构）
- ❓ "个人 agent 资产沉淀" 是否会成为新卖点？（看 2026 年 OpenAI / Anthropic 是否有相关产品）

**对 china-ai-frontline 的启示**：
- 内容选题上，"工具型 agent vs 平台"是 2 年的持续话题
- 写作角度：避开"颠覆/革命"叙事，专注"分层共存 + 个人深度"
- 实操建议：把 OpenClaw / Hermes / Reasonix 当作"个人深度"赛道的资产来运营

eval: todo | 待观察 6-12 个月字节动向 + 工具型 agent 用户数据


---


## 2026-06-04 22:30

来源：抖音直播间观察（一主播卖 OpenClaw 龙虾安装教程，1500 元/课，1000+ 人在线）+ 系统调研

看到有主播在直播间卖 AI 教程——基本就是 OpenClaw 龙虾的安装方法——1500 多一节课，1000 多人看。这说明光是卖信息差也能挣到钱。

诞生想法：用数字人直播卖信息差课程。内容包括 OpenClaw、Hermes、Visnex 的安装方法，桌面配置，以及如何用 AI 提效。目标受众是现在想进入 AI 但技术基础薄弱的人。

### 调研结果速览

**市场验证 ✅**：李一舟 199 元课年销 25 万份（5000 万营收），OpenClaw 龙虾课从 39.8 元到 18999 元，**宝妈是主力购买人群**。定价体系：引流层(0-39.8) → 转化层(99-199) → 利润层(300-1500) → 高客单(2000-18999)。

**观众画像**：宝妈/主妇(~35%)、个体户/小老板(~25%)、职场白领(~20%)、学生(~10%)、技术爱好者(~10%)。共同特征：技术基础低，焦虑是最大购买驱动力。

**数字人技术选型**：硅基智能（推荐，¥399/月起，中文嘴型 95%+，支持实时直播弹幕互动）> 蝉镜 > 腾讯智影。HeyGen/D-ID 无直播支持。开源备选：heygem.ai（硅基开源）、EchoMimic V2（阿里）。

**课程设计草案**：模块 1 AI Agent 全家桶安装（¥99 引流）、模块 2 工作流提效实战（¥299 起）、模块 3 AI 副业入门（¥499 起）。

**直播工作流**：准备阶段（脚本+数字人克隆+课件+背景）→ 开播阶段（OBS 推流+数字人主述+弹幕自动回复+真人盯转化节点）→ 下播复盘（数据+A/B 测试+迭代）。

**平台合规**：⛔ 抖音禁止无人直播；⚠️ 快手/视频号允许但需标注，无流量扶持。

我想让 AI 帮我做：

- 选一个数字人平台（推荐硅基智能），注册+克隆形象+做第一条测试视频
- 完善课程大纲，对应每个模块产出具体脚本
- 写第一场直播的完整话术脚本（含产品介绍+QA+逼单话术）
- 研究视频号/抖音的数字人标注合规流程

eval: missing-task | 待数字人平台注册+课程脚本+合规研究


## 2026-06-05 17:30

来源：实测一个桌面主动型 AI Agent 项目后的延伸思考

今天测试了一个项目，能让 AI Agent 主动和正在电脑前工作的我交谈——它能通过桌面浏览器看到我在做什么，甚至能通过摄像头看到我本人。

这让我开始想下一步：如果 Agent 能看到我，目前它还只是被动聊天。但如果它能识别我的工作状态，在合适的时机用它的专业知识主动插话、干预我正在做的手头工作，或者提出改进建议——这会是一个「主动型 Agent」的形态。

主动型 Agent 的核心特征：
- 有视觉感知能力（桌面 + 摄像头）
- 有时机判断能力（什么时候适合打断）
- 有专业知识储备（知道该提什么建议）
- 有工具调用能力（看到了就能操作，而不仅仅是聊天）

至于工具调用这一块有多大价值，我还不能完全确认，但我认为一个主动的 Agent 会有很大的前景。

我想让 AI 帮我做：

- 找现有主动型 Agent 的实现项目和论文（不只是 chatbot，是能主动发起对话并干预用户工作流的 Agent）
- 调研这种主动型 Agent 在哪些场景下有深刻价值（编程辅助？设计 review？写作 coach？会议旁听？实时翻译？）
- 调研桌面主动型 Agent 的几种演变方向（从「被动响应」到「主动建议」到「主动执行」的路径，以及各阶段的技术前提）

eval: missing-task | 待调研主动型 Agent 实现

## 2026-06-06 14:30

来源：@Fluyeporlaweb 推文（Hermes Skills Hub） + x402-foundation 调研

x402 协议是 HTTP-native 支付标准（Coinbase 起源，x402 基金会维护）。机制：客户端发请求 → 服务端 402 Payment Required → 链上付 → 返回数据。整流嵌入普通 HTTP 往返，零支付中介。已支持 EVM/SVM/Stellar/Algorand/Aptos/Monad。SDK：TS/Python(`pip install x402`)/Go。

HermesHub 用 x402 实现 Creator Marketplace："95% payout to crypto wallet, 0 中介"。一个 skill 作者可以声明 `GET /api/install 收 0.01 USDC`，下载者链上付 → 拿 license key。

组合观察：MCP (Model Context Protocol) 让 agent 发现工具 + x402 让 agent 付费获取服务 = "agent 自主经济"基础设施层。HTTP 402 状态码是 1990s 预留但从未激活的，现在因为加密支付 + agent 浪潮正在复活。

行业里程碑信号：Coinbase、Hyperbolic、Jatevo、AnkanMisra 已接入 x402。MCP + x402 = agent 付费时代。

我想让 AI 帮我做：

- 写成 china-ai-frontline #006 候选文章 "x402 + MCP = agent 付费时代的开始"
- 包含：HTTP 402 历史背景 + Coinbase 起源故事 + x402 工作机制图示 + HermesHub creator marketplace 案例 + 预测未来 1-2 年 agent 经济规模
- 可选：调研国内是否有 x402 兼容的支付方案（如支付宝/微信链上版）
- 可选：实测安装 pip install x402 跑通 demo

eval: missing-task | 建议加：调研 MCP + x402 是否已有 RFC/正式标准

## 2026-06-10 16:30

来源：微信群关于 Claude Code 模型选择 / 成本控制的讨论 + [`slaymaker1907/claude-code-model-picker/SKILL.md`](https://github.com/slaymaker1907/claude-code-model-picker/blob/main/SKILL.md) + 4 张截图记录

今天微信群里聊到一个很具体的问题：Claude Code 里到底什么时候该切模型？有人关心的是效果，有人关心的是成本，也有人已经遇到过“上了强模型但并没有明显变好，反而烧得更快”的情况。

这个讨论真正有意思的地方不在于“哪个模型最强”，而在于它暴露了一个更底层的问题：

**我们现在还缺一套模型调度的使用常识。​**

随后看到一份很有意思的 Claude Code 调度 skill：`model-picker`。

它表面上只是一个“帮你选模型和 thinking level”的 SKILL.md，但真正有价值的不是推荐哪个模型，而是它把一个很模糊的问题拆清楚了：

**什么时候该切模型？什么时候不该切？切模型到底贵在哪里？​**

我以前对“切模型”的理解比较粗：任务复杂了就上更强模型，任务简单了就换便宜模型。但这份 skill 给了一个更精确的判断框架：

**不要把“换模型”当成第一反应。​**

因为在 Claude Code 这类 agentic coding session 里，切模型不是只换一个推理引擎。它会带来 cache 失效：system prompt、tool definitions、message history 都要重新写入，而且 cache write 还有额外成本。也就是说，模型切换本身也是一笔账。

所以第一条结论是：

**能调 thinking level，就先不要切模型。​**

如果只是当前模型思考不够，优先把同一个模型的 thinking level 往上调。因为同模型改 thinking level 通常能保住一部分缓存，尤其是 system prompt 和 tool definitions，不会像跨模型切换那样整段上下文重写。

第二条结论是：

**切模型应该看失败信号，不应该看“感觉任务高级”。​**

这份 skill 里列的几个切换信号很实用：

- 连续 tool-call 失败，反复 retry 同一个操作；
- 多文件修改时开始丢上下文，前后逻辑接不上；
- plan 和 implementation 漂移，写出来的代码和原意不一致；
- agentic loop 里不断修错，但越修越偏。

这些不是“模型不够聪明”的抽象判断，而是可以观察到的运行时症状。也就是说，切模型不是因为“我要做大事”，而是因为当前模型已经在执行中表现出明显失配。

第三条结论是：

**长上下文下，一次性切强模型不一定划算。​**

如果 session 已经很长，只想让更强模型帮忙看一两轮，比如做一次架构判断、一次复杂 plan、一次 review，那更好的方式可能不是在原 session 里直接切模型，而是开一个 fresh thread，把上下文摘要贴过去，让强模型只做那一轮判断，再把结果带回原 session。

这其实是一个非常重要的使用技巧：

**强模型不一定要接管整个工作流，它可以只负责关键判断。​**

第四条结论是：

**高端模型低 thinking 不一定比中端模型高 thinking 强。​**

这点反直觉。很多人会默认“更强模型的低档位，也比普通模型高档位强”。但这份 skill 的判断是，在 agentic coding 这种需要持续规划、工具调用、纠错和上下文保持的任务里，thinking headroom 很关键。一个 thinking 被压得很低的强模型，可能不如一个 thinking 开足的中端模型稳定。

这背后的经验是：

**agent 任务不是单轮问答，便宜失败会变贵。​**

一次错误输出的成本不只是那一轮 token，而是后续反复修正、重复工具调用、重写上下文、重新找回任务意图的总成本。表面上省了推理费，实际可能在失败循环里烧掉更多。

第五条结论是：

**最有效的模式可能是“强模型做 plan，中端模型做 execution”。​**

这份 skill 里提到一个很清楚的分工：用更强模型做架构规划、跨文件分析、复杂设计判断；等 plan 稳了，再切回更便宜、更适合执行的模型去改代码、跑测试、做机械编辑。

这和我之前写 Fable 5 的判断能接上：

**短任务看能力，长任务看链条。​**

模型越强，越不应该被无脑调用。真正高效的用法不是“所有任务都上最强”，而是把不同模型放在不同位置：

- 强模型：负责 plan、结构判断、复杂推理、关键 review；
- 中端模型：负责执行、改文件、跑测试、常规 debug；
- 低成本模型：负责搜索、整理、格式转换、一次性机械任务；
- 同模型 thinking level：作为比“切模型”更轻量的调节旋钮。

这份 `model-picker` skill 的价值在于，它把模型选择从“玄学手感”变成了一个可执行的调度策略。

我想后面可以写一篇小文章，主题不是“哪个模型最强”，而是：

**会用模型的人，不是永远用最强模型的人，而是知道什么时候该换、什么时候不该换、什么时候只该让强模型出一轮主意的人。​**

这也可以延伸到 agent 工具链的长期判断：

当模型价格越来越高、上下文越来越长、agent loop 越来越复杂，真正重要的能力会从“调用最强模型”变成“调度模型组合”。模型路由、thinking level、cache 成本、上下文摘要、plan/execution 分工，会成为下一阶段 AI 使用者和工具链之间的核心分水岭。

想让 AI 帮我做：

- 把这份 `model-picker` SKILL.md 拆成一篇中文短文：主题是“什么时候该切模型？”
- 提炼一个模型调度决策树：先调 thinking level，还是切模型，还是开 fresh thread？
- 结合 006 Fable 5 的“强模型 + 高成本”判断，写一条 X thread 或公众号短段落，讨论“前沿模型时代，真正稀缺的是调度能力”
- 如果素材够，做成一篇 007 Agent 工具链文章里的小节：模型越强，越需要路由；模型越贵，越需要预算控制；模型越不透明，越需要观测层

eval: todo | 可升级为 007 Agent 工具链素材：模型调度 / thinking level / cache 成本 / plan-execution 分工

## 2026-06-13

来源：对"AI 驱动产业平台"选题的主动扩展 — Xometry 是全球第一个跑通的 AI 驱动工业制造平台

Xometry 是做什么的：一个 AI 驱动的按需制造平台。客户上传 CAD 图纸 → AI 即时报价+可制造性分析 → 匹配全球制造网络中的供应商 → 完成生产交付。覆盖 CNC 加工、3D 打印、注塑成型、钣金加工等工艺。2021 年纳斯达克上市（NASDAQ: XMTR）。

这为什么值得调研：
- 它是少数把"AI 报价引擎"真正跑通的 B 端案例——不是 chatbot，是直接赚制造服务的钱
- AI 在这件事上的核心角色是：即时解析 CAD 几何特征 → 判断可制造性 → 匹配产能 → 定价。本质是**把工程师的知识工作变成了 API**
- 这是 AI agent 经济的一个极端但真实的 anchor case：制造业可能比内容/电商更早跑通"AI 驱动平台"的模型，因为制造业的问题是**几何可解析、工艺可穷举、供应商有产能约束**——恰好落在 AI 的强项

询报价流程与技术内核（重点剖析）：

这是 Xometry 最值得研究的环节。制造业询报价的传统流程是：客户发 CAD 图纸 → 工厂工程师人工读图 → 判断能不能做、用什么工艺做 → 手写出报价。这个流程慢（几天到一周）、贵（工程师工时）、不一致（不同工厂报价差几倍）。

Xometry 把这个流程压到了**即时**。拆成三层来看：

**第一层：客户侧 — 即时报价引擎（Instant Quoting Engine）**

客户上传 3D CAD 文件（STEP/IGES/SLD 等格式）→ 平台在几秒到几分钟内返回：
- 单价（分数量梯度：1 件 / 10 件 / 100 件……）
- 可制造性反馈（DFM：壁太薄？深孔？倒扣？）
- 推荐工艺（CNC 还是 3D 打印更合适？）
- 预计交期

这个引擎的核心能力不是"查价格表"，而是**几何特征自动识别**——AI 从 CAD 模型中提取几何特征（孔径、壁厚、深宽比、曲面复杂度、公差要求），然后根据特征与工艺规则的映射关系计算工时和材料成本。

**第二层：平台侧 — 供应商匹配与定价（Supplier Network + Dynamic Pricing）**

客户接受报价后，平台不自己生产，而是把订单派给全球制造网络中的供应商。这一层有三个技术问题：
- **怎么给供应商定价**：平台给客户的是 Xometry 的报价，给供应商的是另一个采购价，中间差价是平台利润。AI 需要学习"每家供应商对同类零件的历史报价 + 产能饱和度 + 交期表现"，才能动态匹配到最优供应商
- **怎么分单**：一个订单可能拆给多家供应商（部分工序产能不够），AI 需要做工序拆分 + 供应商组合优化
- **怎么保质量**：供应商交付的零件由 Xometry 质检后再发给客户，平台承担品质担保责任

**第三层：数据飞轮 — 为什么竞品难追**

Xometry 从 2013 年开始积累数据，核心护城河不是算法而是**配对数据**：
- 每一笔交易 = 一个 CAD 模型 + AI 报价 + 实际生产成本 + 供应商表现 + 质检结果
- 几十万笔交易训练出来的报价模型，会对"这个公差等级在 CNC 上大概需要多少工时"有越来越准的预测
- 新入局者即使有同样的 AI 技术，没有这个数据集也报不准价——报低了亏钱，报高了丢单

所以 Xometry 的本质不是"制造业的 Uber"，而是**数据驱动的定价基础设施**——它用 AI 把制造业报价这个高度依赖工程师经验的过程标准化、即时化、可规模化了。

对照中国：嘉立创的 PCB 报价是规则引擎（工艺参数少、标准化程度高），但 CNC/注塑这类非标工艺的即时报价还没人跑通。难点不在平台模式，而在**制造报价数据集**的积累——中国工厂的报价数据分散、不标准化、不共享。

关键调研问题：

1. **Xometry 平台画像**：它到底是什么——制造中介？SaaS 平台？分布式工厂？它的供应链模式是自建产能还是纯 marketplace？
2. **服务的客户是谁**：大企业还是中小制造商？什么行业最多（航空航天、医疗、汽车、消费品？）？客户什么规模、什么角色（采购经理、设计工程师、产品经理？）
3. **AI 报价引擎的深度**：AI 到底做到了什么程度——只是参数化报价，还是真的能识别几何特征并推理工艺路径？训练数据从哪来？
4. **竞争格局**：它的竞争对手是谁——Protolabs、Hubs（被 Protolabs 收购）、Fictiv、Fast Radius？差异化在哪？（AI 先发 vs 传统人工报价 vs 自有产能）
5. **中国市场对照**：
   - 中国有没有 Xometry 的参照者？嘉立创（PCB/3D 打印）、未来工场、云工厂、速加网——谁最接近？
   - 中国制造业的特点（富余产能、低价竞争、关系型供应链）是否让"AI 自动匹配产能"这件事在中国更难还是更容易？
   - 如果中国没有真正的 Xometry 对照，缺的是什么——AI 报价引擎的积累？制造网络标准化？客户信任？还是市场不需要？
6. **和 china-ai-frontline 选题的相关性**：
   - 如果 Xometry 证明 AI 在制造业能跑通平台模型，那它和蚂蚁 AI 支付的三阶段框架（第二阶段 Agent 工具链 → 第三阶段入口争夺）是什么关系？
   - 制造业 AI 平台会是比电商更早跑通的"AI 平台经济"案例吗？
   - 能否作为 007 Agent 经济回路文章的一个产业对照案例？

想让 AI 帮我做：
- 搜 Xometry 的上市招股书（S-1）和最近两个季度的财报，提取客户数量、客单价、AI 引擎的技术描述、毛利率
- 搜 Protolabs / Fictiv / Hubs 的对比分析文章
- 搜中国制造业 AI 平台（嘉立创、云工厂等）的信息，做中美对照
- 判断是否值得单独开选题（可能归入 pillar-X 观察线或作为 Agent 经济回路的产业 case）

eval: todo | 需要联网调研 + S-1 阅读 | 潜在方向：Agent 经济回路的产业对照案例 / 独立 pillar-X 选题
